import { FC } from "react";
import { Header } from "@components/composite/Header";

const MainLayout: FC = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default MainLayout;
